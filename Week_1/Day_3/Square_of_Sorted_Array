class Solution {
    public int[] sortedSquares(int[] arr) {
         int n = arr.length;
        int[] ans = new int[n];
        for(int i=0;i<n;i++){
            int sq =  arr[i] * arr[i];
            ans[i] = sq;

        }
        Arrays.sort(ans);
        return ans;
    }
}